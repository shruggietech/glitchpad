# Type specimen

`glitchpad-type-specimen.svg` demonstrates the three-family hierarchy and technical character set. Every glyph is converted to a filled vector path. The file contains no live text and is safe for deterministic review or export.

The PNG is a convenience preview. The SVG remains the source of truth.
