# Glitchpad v0.1.2 Corrective Release (unreleased)

Glitchpad v0.1.2 is the planned desktop rendering correction following v0.1.1. S030 keeps the requested document usable when Markdown rendering fails, prevents document source from appearing while a preview is pending, stabilizes the compact menu clear of document scrollbars, and removes the redundant Platforms badge from the repository header.

## Planned behavior delta

- A document-scoped failure boundary keeps the application shell available and offers explicit source recovery for Markdown.
- Markdown render results are accepted only for the current session, revision, and render generation.
- Rendered mode shows a constant pending state without exposing source-derived content.
- The application menu trigger remains fixed at the upper-left and its popup does not participate in document layout.
- The README retains its supported-platform table while dropping the oversized Platforms badge.
- Packaged Windows validation exercises two complex Markdown documents in both open orders and records active-content evidence.

## Issue traceability

- #160: eliminate blank Markdown viewports and preserve usable content across sequential opens.
- #161: prevent raw Markdown source from flashing while rendered preview is pending.
- #162: keep the application menu trigger stable and clear of the document scrollbar.
- #163: remove the redundant Platforms badge without weakening platform documentation.

## Release boundary

This document records an unreleased corrective delta. It does not publish v0.1.2, change the current v0.1.1 package identity, or broaden the supported-format claims.
