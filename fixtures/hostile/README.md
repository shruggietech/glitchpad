# Hostile Fixtures

This directory holds inert adversarial inputs for bounded parser harnesses. Never open these files through an operating-system shell or unrelated viewer; tests must identify their parser boundary, resource limit, expected rejection or degradation result, and provenance record.

No hostile fixtures are committed at v0.0.0.
