# Quickstart: Validate S040

## Prerequisites

Use the repository-pinned validation image built from `scripts/docker/validation.Dockerfile`; Windows commands cross `scripts/invoke-docker-hidden.ps1`, and Git/GitHub use `scripts/invoke-vcs-hidden.ps1` with explicit argument arrays. Official product/support authority stays 0.1.3.

## Focused Scenarios

1. Open original SVG fixtures and each active/external/pathological fixture. Verify inert native PNG or a classified bounded fallback, declared font policy, and zero network/navigation/script/native escape.
2. Open GIF/animated WebP paused, step every golden frame, compare disposal/blend pixels and original timing/loop facts, play explicitly, enable reduced motion, switch/background/close, and verify no new work/timer/retained decoder surfaces.
3. Open mixed PNG/BMP/malformed/duplicate ICO entries, inspect every row, preview each valid entry, then export one selected PNG. Verify cancellation, source/destination alias denial, conflicts, source revision changes, provider errors and unchanged original bytes.
4. Exercise all advertised controls with keyboard/touch/pinch/assistive technology at four desktop/mobile viewports; retain selection/viewport on session restoration while remaining paused.

## Gates

Run focused native family/host/export and frontend gateway/control/persistence tests, fixture digest/layout checks, dependency/license/notices, and complete `cargo xtask check`. Watch actual completion and exit status. Hosted Windows/macOS/Linux and API 24/36 evidence must pass on the final PR head; reply to/resolve every review finding and request no more than one explicit follow-up round. Keep the official PR unmerged for the owner.

Full image milestone fuzz/conformance and public activation remain #75/#76 and are not implicitly closed by these focused scenarios.
