# Verification: BrandBuilder AppFrame Adoption

## Traceability and authority

- Upstream work slice: S042, issue [shruggietech/shruggie-brand#219](https://github.com/shruggietech/shruggie-brand/issues/219), pull request [shruggietech/shruggie-brand#231](https://github.com/shruggietech/shruggie-brand/pull/231), merged to `main` as `ac15435da2118db321ab52939aff1bac830c202b`, plus focused desktop-resize correction pull request [shruggietech/shruggie-brand#232](https://github.com/shruggietech/shruggie-brand/pull/232).
- Downstream adoption: issue [shruggietech/glitchpad#196](https://github.com/shruggietech/glitchpad/issues/196), pull request [shruggietech/glitchpad#197](https://github.com/shruggietech/glitchpad/pull/197).
- Consumer baseline: `3d9a56048d01bbe2e661933e2c6eaebc7526534b`. Adopted upstream candidate: `65ed38adea0462a8bc744bff8a19ec55a738c4c4`.
- Artifact authority: successful Build run `35498773758`, artifact `10600989362`, named `verified-brand-kits-8ceb22f700d29595c4967a54973d5df0b1571fbe`. GitHub built the pull-request artifact at synthetic merge commit `8ceb22f700d29595c4967a54973d5df0b1571fbe` from the adopted candidate head.
- Contract identity: Glitchpad brand `1.1.0`, Brand Canon `1.2.1`, generated React/Vite renderer adapter, Tauri host, 313 governed files.
- Authority order: the successful SHA-qualified workflow artifact, its governed `manifest.json`, Glitchpad's immutable `brand/INTEGRATION.json` receipt, then generated kit guidance.
- Recovery: rerun `scripts/sync-brand-kit.mjs` with the recorded source revision, workflow run, artifact identity, and retrieval date. Never reconstruct or hand-edit governed files under `brand/`.

## Baseline

Before adoption, Glitchpad's product-local `.app-shell` element was also the semantic `main` landmark, while `html`, `body`, and `#root` duplicated height and overflow ownership in `apps/glitchpad/src/styles.css`. The imported pre-S042 kit had no generated React AppFrame surface. Historical host timing and prior review-round duration were not captured, so this slice establishes a prospective baseline instead of inventing measurements.

## Test progression

- Red: AppFrame composition and exact-agent-contract assertions could not pass against the pre-S042 kit because the generated adapter and enforcement contract were absent.
- Review red: the downstream second-round Codex review identified that any focused desktop window-height change was misclassified as an IME inset. The focused regression reproduced the defect against the previously imported kit, reporting `--bb-ime-block-end: 200px` after an ordinary desktop resize where `0px` was required.
- Green: the exact successful final S042 artifact imported with all 313 governed files verified. Brand governance passed 24 tests plus the full manifest, provenance, agent-contract, integration, encoding, and license checker. Documentation formatting and 434-file link validation passed, including the generated absolute hosted-manual link. The AppFrame product suite passed 29 tests; typecheck and production build passed; the production-CSS shell matrix passed 48 cases covering three viewports, mouse and coarse pointers, tabs, light and dark themes, forced colors, reduced motion, display scales, browser zoom, and 100-200 percent root text scaling.
- Review green: upstream follow-up candidate `65ed38a` replaced content and OS-name inference with the explicit `(hover: none) and (pointer: coarse)` capability contract and retained orientation baselines only where layout resize can represent IME movement. The SHA-qualified artifact imported with 313 governed files verified, brand governance passed 24 tests, and the focused desktop regression passed with `--bb-ime-block-end: 0px`.
- Local aggregate observation: the full `cargo xtask check` passed every Rust, Android-source, policy, lint, and typecheck stage, then the Windows bind-mounted validation container failed to start Vitest workers for `resource-ledger.test.ts` and `performance.test.ts` after unusually slow filesystem I/O. The focused AppFrame suite and production build were green in the same environment, and the native shared frontend CI job passed. This local infrastructure result is recorded rather than substituted for CI.

## Host evidence

- Android: the initial PR run `35457241343` reached the actual Tauri WebView on API 24 and API 36. Both hosts passed portrait and landscape AppFrame ownership before menu disclosure, then reproduced the reviewed headerless-row defect when the disclosed menu moved outside the reported visual viewport. Run `35459533690` proved the corrected headerless row, portrait and landscape ownership, and menu reachability on both API levels before finding that the IME probe did not publish a positive inset. Runs `35461108985`, `35485438545`, `35487248042`, `35489387617`, and `35491373204` successively isolated a transient Gradle DNS outage, instrumentation teardown, the missing pre-WebView-139 resize bridge, programmatic focus without a user gesture, and Espresso's inability to inject into the Android 16 headless window because it never reported window focus. The final system-level touchscreen gesture preserves a real touch at the centered WebView editor without depending on Espresso's root-focus precondition. Final CI run `35493009456` at downstream head `d690d63d98c78ec5570a66de30b7b31529641e3e` passed API 24 job `106031058295` and API 36 job `106031058370`, then passed aggregate job `106035490186`. The API 24 receipt records one Tauri AppFrame, one main landmark, portrait visual height 640, landscape visual height 320, a focused editor with `imeBlockEnd` 270 and resized visual height 370, reachable closed and open menus, and no cutout API. The API 36 receipt records the same single-owner contract, portrait safe-area padding 48, landscape safe-area padding 48 on the physical left, a focused editor with `imeBlockEnd` 294 and resized visual height 346, reachable closed and open menus, and cutout safe insets `[48,0,0,0]`. Both receipts record hidden root and body overflow, valid viewport values, menu and popup containment, and the IME inset returning to zero before orientation evidence.
- Android retry hardening: run `35496298253` exposed a one-shot API 36 IME request racing the input connection, while run `35497805712` proved that indiscriminate retries and repeated WebView focus requests could steal API 24 DOM focus. The corrected harness preserves the real editor focus, issues one legacy request on API 24, and limits bounded retries to API 30 and later. The Android app and instrumentation APK compile against that final source, and the focused native conformance suite passes 4 tests with 1 reference-host test intentionally ignored.
- Windows: the initial PR and push package runs `35457241332` and `35457224247` built the real NSIS candidate and reached the installed lifecycle, where UI Automation could not discover the editor after the headerless content track collapsed. Corrected Windows package runs `35459533686` and `35459531344` passed the real package, installed, and portable lifecycles. Browser geometry remains supporting evidence only.

## Observations and limitations

- The downstream app deliberately uses the bounded generated `full-bleed` layout because Glitchpad already owns its document-local scrolling and compact shell chrome.
- Tauri retains native window/titlebar authority. AppFrame owns only web safe-area, visual viewport, IME, and root layout geometry.
- API 24 has no platform display-cutout API. Its evidence records that limitation while still proving the same AppFrame, orientation, IME, focus, and menu contract.
- Historical baseline timings, prior correction counts, and fresh-session measurements were not recorded at `3d9a560`, so no retrospective numbers are invented. Post-adoption observations currently include one automatic upstream Codex review round with three corrected findings, two integration-driven upstream corrections (legacy Android WebView geometry and complete site adapter staging), one downstream provenance correction separating the adopted head from GitHub's synthetic artifact commit, and one automatic downstream Codex review round with two accepted findings (headerless AppFrame row placement and repository-relative generated guidance). The downstream second review found the focused-desktop-resize defect, and the focused upstream follow-up review then replaced a user-agent-derived correction with a capability contract before artifact publication. Android convergence used evidence-driven follow-up runs to narrow host and harness conditions without weakening the AppFrame acceptance criteria.
- The final official artifact import, including Docker startup and 313-file verification, completed locally in 7.3 seconds. Deterministic brand validation and 24 contract tests completed in 4.0 seconds. Final run `35493009456` passed the shared native and frontend gate, documentation, dependency review, licenses and secret patterns, Android debug build, desktop hosts, performance smoke, API 24 and API 36 device evidence, and aggregate `ci-ok`. The API 36 reference job took 38m34s and API 24 took 16m49s. The local Android app and instrumentation APK build also completed successfully against the final test source. The final uninterrupted `cargo xtask check` completed with exit status 0 from an isolated native Linux volume after installing the lockfile's exact Playwright and Puppeteer browsers; it passed Rust formatting, Clippy, tests, dependency policy, 51 frontend files and 300 tests, production builds, 73 site browser tests, all brand and platform-package contracts, configuration, formatting across 402 Markdown files, links across 433 Markdown files, 47 Mermaid renders, and UTF-8/BOM/mojibake validation across 1,873 text files. A prior focused Rust test attempt in the Android packaging image could not link desktop GTK/WebKit libraries, so its result is not counted as product evidence; the exact source subsequently passed the complete native gate in the validation image.

## Merge order

Merge upstream follow-up pull request #232 first. Merge downstream pull request #197 only after #232 is merged, its manual second Codex review is closed, the downstream second-review thread is closed, and the final pushed downstream head is green. The downstream receipt remains pinned to the reviewed upstream candidate commit and successful artifact, preserving deterministic recovery independently of the eventual merge commit.
